pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '''#!/bin/bash
                    ls
                    cd exercise-11/jenkins/project/flask
                    docker build -t my-flask:${GIT_COMMIT:8} .
                '''
                
            }
        }
    }
}