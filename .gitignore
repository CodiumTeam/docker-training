.idea
scan.log

# to avoid comitting any changes to the state of the initial DB
# if you change the initial DB image you can still add it with
# git add -f
./exercise-12/jenkins/jenkins-runner/gogs/gogs.db
